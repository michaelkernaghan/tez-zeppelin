import { DAppClient } from '..'; // Replace '..' with '@airgap/beacon-sdk'
const client = new DAppClient({ name: 'My Sample DApp' });
client
    .requestPermissions()
    .then((response) => {
    console.log('permissions', response);
})
    .catch((permissionError) => console.error(permissionError));
//# sourceMappingURL=permission-request.js.map